
import configparser
import maya.cmds as cmds

# Create the config object on the module

# create guides off of config modules that are set and customised by the user. 

# Instead of storing attr on the guides cr ini files w/ a databse to 
# temporaryly store the rigging settings